<!doctype html>
<html lang="fr" class="h-full bg-white">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    @entryPointStyles('app')
    @entryPointScripts('app')

    <link rel="stylesheet" href="https://cdn.quilljs.com/1.3.6/quill.snow.css">
    <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/10.1.2/highlight.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>

    <title>{{ title }}</title>
  </head>
  <body x-data="{ modal: false }" class="h-full" layout-root>
    <div x-data="{ open: false }" class="min-h-full hidden md:block" layout-navbar>
      @!component('components/admin/navbar')
      <!-- Main column -->
      <div class="lg:pl-72 flex flex-col">
        <!-- Search header -->
        <main class="flex-1" layout-main>
          <div class="border-b border-gray-200 px-4 py-4">
            <div class="flex-1 min-w-0">
              <h1 class="text-lg font-medium leading-6 text-gray-900 sm:truncate">{{ title }}</h1>
            </div>
          </div>
          <div>
            @!section('body')
            
          </div>
        </main>
      </div>
    </div>
    <div class="md:hidden">
      <div class="flex-1" layout-main>
        <div class="border-b border-gray-200 px-4 py-4 sm:flex sm:items-center sm:justify-between sm:px-6 lg:px-8">
          <div class="flex-1 min-w-0">
            <h1 class="text-lg font-medium leading-6 text-gray-900 sm:truncate">{{ title }}</h1>
          </div>
          <div class="mt-4 flex sm:mt-0 sm:ml-4">
            @!section('actions')
          </div>
        </div>
        <div>
          @!section('body')
        </div>
      </div>
      @!component('components/admin/navbar-mobile')
    </div>
  </body>
</html>